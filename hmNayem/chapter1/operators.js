// !JavaScript All operators
/*
Arithmetic Operator     = (+, -, *, /, %, ++, --)
Assignment Operator     = ()
*/

// ? Arithmetic Operator
// addition


// modoluos 
var a = 10;
var b = 3;
console.log(a % b);

// ?Increment, when its (k++ post increment but still the value, when we re declare to the console than this value will be changed), when its (++k pre increment instant fall the value)
var k = 10 // its still the value
k++ //its instant increase the value
console.log(k);

// ?Decrement, when its (l++ post decrement but still the value, when we re declare to the console than this value will be changed), when its (++l pre decrement instant fall the value)
var l = 20; //its still the value
l-- //its instant decrease the value
console.log(l);










// ? Assignment Operator





// ? comparesion  Operator

